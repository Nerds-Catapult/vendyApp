

export interface expectedBusinessProps {
    id: string | number;
    name: string,
    phoneNumber: string;
    email: string;
    address: string;
    city: string;
    country: string;
    image: string | null;
}